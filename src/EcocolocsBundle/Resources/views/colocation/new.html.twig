{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter une nouvelle colocation</h1>
<div class="container">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="center">
        <input class="btn waves-effect" type="submit" value="Valider" />
    </div>
    {{ form_end(form) }}
</div>
    <div class="center">
        <a class="btn waves-effect" href="{{ path('colocation_index') }}">Retourner à la liste des colocations</a>
    </div>
{% endblock %}
